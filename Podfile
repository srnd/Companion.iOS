platform :ios, '11.0'

target 'CodeDay Companion' do
  use_frameworks!

  pod "BarcodeScanner"
  # pod "MTBBarcodeScanner"
  pod "Alamofire"
  pod "AlamofireImage"
  pod "Disk"
  pod "QRCode"
  pod "SwiftDate"

  target 'CodeDay CompanionTests' do
    inherit! :search_paths
  end

  target 'CodeDay CompanionUITests' do
    inherit! :search_paths
  end
end
